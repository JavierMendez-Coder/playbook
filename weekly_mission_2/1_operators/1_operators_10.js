// Example 11: find() to get the first element of a list that pass the test implemented
const ages = [24, 22, 19, 25, 32, 35, 18];
const age = ages.find((age) => age < 20);
console.log("Example 11: Which is the first age under 20? " + age);
