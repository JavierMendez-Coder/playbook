// Example 2: forEach() to calculate the sum of each element from a list
let sum = 0;
const numbers = [1, 2, 3, 4, 5];
numbers.forEach((num) => (sum += num));
console.log(
  "Example 2: forEach to calculate the sum of each element from a list"
);
console.log(sum);
